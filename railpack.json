{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "steps": {
    "install": {
      "commands": ["...", "python -m playwright install --with-deps chromium"]
    }
  },
  "deploy": {
    "aptPackages": [
      "...",
      "xvfb", "xauth", "dbus"
    ],
    "startCommand": "mkdir -p /tmp/.X11-unix && Xvfb :99 -screen 0 1920x1080x24 & export DISPLAY=:99 && gunicorn --bind 0.0.0.0:$PORT --workers 1 app:app"
  }
}
